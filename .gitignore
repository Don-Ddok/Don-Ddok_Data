# 은행 원본 데이터 — 절대 커밋 금지
*.xlsx
*.xls

# 은행 데이터에서 파생된 중간 파일(회사 단위 값 포함) — 커밋 금지
*.parquet
*.feather
*.pkl
*.db
*.sqlite
/data/raw/**
/data/processed/**
!/data/raw/.gitkeep
!/data/processed/.gitkeep

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# 환경
.env
